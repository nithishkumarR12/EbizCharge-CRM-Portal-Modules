<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="view_product_product_ebizcharge" model="ir.ui.view">
            <field name="name">product.template.form.inh</field>
            <field name="model">product.template</field>
            <field name="inherit_id" ref="product.product_template_only_form_view"/>
            <field name="arch" type="xml">
                <xpath expr="//header" position="inside">
                    <field invisible="1" name="auto_sync_button"/>
                    <field invisible="1" name="ebiz_product_internal_id"/>
                    <button class="oe_highlight" invisible="auto_sync_button or ebiz_product_internal_id" name="add_update_to_ebiz_ind" string="EBizCharge Upload" type="object"/>
                </xpath>
                <field name="type" position="before">
                    <field domain="['|',('company_ids', 'in', ebiz_company_ids), ('id', 'in', ebiz_profile_ids)]" name="ebiz_profile_id" options="{'no_edit': True, 'no_create': True, 'no_open': True}" readonly="ebiz_product_internal_id != False" string="EBizCharge Merchant Account"/>
                </field>
                <field name="barcode" position="after">
                    <field name="upload_status"/>
                    <field invisible="1" name="auto_sync_button"/>
                    <field invisible="1" name="ebiz_company_ids" widget="many2many_tags"/>
                    <field invisible="1" name="ebiz_profile_ids" widget="many2many_tags"/>
                </field>
            </field>
        </record>

 

        <record id="product_new_action1" model="ir.actions.server">
            <field name="name">EBizCharge Upload</field>
            <field name="type">ir.actions.server</field>
            <field name="model_id" ref="model_product_template"/>
            <field name="binding_model_id" ref="model_product_template"/>
            <field name="state">code</field>
            <field name="code">action = records.add_update_to_ebiz()</field>
        </record>

    </data>
</odoo>
